<h2>Passengers</h2>

<table>
  <tr>
    <th>
      Passenger ID
    </th>
    <th>Passenger</th>
  </tr>
<% @passengers.each do |passenger| %>
  <tr>
    <td>
      <%= passenger.id %>
    </td>
    <td>
    <%= link_to(passenger.name, passenger_path(passenger.id)) %>
    </td>
  </tr>
<% end %>
</table>
