<h1>Drivers</h1>
<ul>
  <% @drivers.each do |driver| %>
  <li class="drivers">
    <span>
      <%= link_to driver.name, driver_path(driver.id), class: 'name-link' %>
    </span>
  </li>
  <% end %>
</ul>
