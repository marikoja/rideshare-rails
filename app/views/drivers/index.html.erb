<h2>Drivers</h2>

<table>
  <tr>
    <th>
      Driver ID
    </th>
    <th>Driver</th>
  </tr>
<% @drivers.each do |driver| %>
  <tr>
    <td>
      <%= driver.id %>
    </td>
    <td>
    <%= link_to(driver.name, driver_path(driver.id)) %>
    </td>
  </tr>
<% end %>
</table>
